# `EZbakRArrowData` EZbakRArrowData validator

`validate_EZbakRArrowData` ensures that input for `EZbakRArrowData`
object construction is valid.

## Usage

``` r
validate_EZbakRArrowData(obj)
```

## Arguments

- obj:

  An object of class `EZbakRArrowData`
